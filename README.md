![trfeed logo](logo.png)

### trfeed

- It uses transmission-remote to add torrents.
- It uses a simple YAML-based configuration file.
- Check the attached configuration sample to see how it works.
- It does not use regex because it has a simpler solution. See below.